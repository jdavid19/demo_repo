version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:8000"
    volumes:
      - ./uploads:/app/uploads         # Persist uploaded files
      - ./static:/app/static           # Serve static files correctly
    environment:
      - PYTHONUNBUFFERED=1
